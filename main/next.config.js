/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash:true,
  experimental: {
    runtime: "experimental-edge",
    },


}


module.exports = nextConfig

